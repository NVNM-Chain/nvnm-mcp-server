apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "inveniam-mcp-server.fullname" . }}-config
  labels:
    {{- include "inveniam-mcp-server.labels" . | nindent 4 }}
data:
{{- range $k, $v := .Values.env }}
  {{ $k }}: {{ $v | quote }}
{{- end }}
